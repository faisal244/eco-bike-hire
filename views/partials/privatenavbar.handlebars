<header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Eco Bike Hire</a>
        </div>
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarNavDropdown"
          aria-controls="navbarNavDropdown"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <a class="nav-link {{#if (isEqualTo currentPage "home")}}active{{/if}}" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/public">Bikes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{#if (isEqualTo currentPage "dashboard")}}active{{/if}}" href="/dashboard">Dashboard</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Logout</a>
              </li>
      </div>
        </div>
      </nav>
    </header>